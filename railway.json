{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python manage.py migrate && python manage.py seed_db && gunicorn retailsense.wsgi --bind 0.0.0.0:$PORT",
    "healthcheckPath": "/api/ingest/status/",
    "restartPolicyType": "ON_FAILURE"
  }
}